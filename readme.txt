Go to the frontend folder and run npm install to install dependencies and npm start to start a local server
and for the database go to the backend folder and run npm install and npm start to get node js api running
